# include < iostream > 
using namespace std ; 
int r [ r ] ; 
long long r [ r ] ; 
bool r [ r ] ; 
long long r [ r ] ; 
long long r [ r ] ; 
void r ( int r , int r ) { 
if ( r = = r ) { 
r [ r ] = r [ r ] ; 
return ; } 
int r = ( r + r ) / r ; 
r ( r , r ) ; 
r ( r + r , r ) ; 
int r = r - r - r ; 
for ( int r = r + r ; r < = r ; r + + ) { 
r [ r ] + = r [ r + r ] ; 
} 
return ; 
} 

void r ( int r [ ] , bool r [ ] , int r , long long r [ ] ) { 
if ( r = = r ) { 
r [ r ] = r [ r ] ; 
return ; } 
int r = r / r ; 
int r [ r ] ; 
int r [ r ] ; 
bool r [ r ] ; 
bool r [ r ] ; 
long long r [ r ] ; 
long long r [ r ] ; 
for ( int r = r ; r < r ; r + + ) { 
r [ r ] = r [ r ] ; 
r [ r ] = r [ r ] ; 

} 
for ( int r = r ; r < r ; r + + ) { 
r [ r - r ] = r [ r ] ; 
r [ r - r ] = r [ r ] ; 

} 

r ( r , r , r , r ) ; 
for ( int r = r ; r < r ; r + + ) { 
if ( r [ r ] ) 
r [ r - r ] = r [ r ] - r [ r - r ] ; 
else 
r [ r - r ] = r [ r ] ; 
} 
r ( r , r , r , r ) ; 
for ( int r = r ; r < r ; r + + ) 
r [ r ] = r [ r ] ; 
for ( int r = r ; r < r ; r + + ) { 
r [ r ] = r [ r - r ] + r [ r - r ] ; 
} 
return ; 
} 

void r ( long long r [ ] , int r , long long r [ ] ) { 
if ( r = = r ) { 
r [ r ] = r [ r ] ; 
return ; } 
int r = r / r ; 
long long r [ r ] ; 
long long r [ r ] ; 
long long r [ r ] ; 
long long r [ r ] ; 
for ( int r = r ; r < r ; r + + ) { 
r [ r ] = r [ r ] ; 
} 
for ( int r = r ; r < r ; r + + ) { 
r [ r - r ] = r [ r ] ; 

} 
for ( int r = r ; r < r ; r + + ) { 
r [ r ] = r [ r ] - r [ r ] ; 
} 
r ( r , r , r ) ; 
r ( r , r , r ) ; 
for ( int r = r ; r < r ; r + + ) 
r [ r ] = r [ r ] ; 
for ( int r = r ; r < = r ; r + + ) 
r [ r ] = r [ r - r ] ; 


return ; 
} 
int r ( ) { 
r : : r ( false ) ; 
r ( r ) ; 
int r ; 
cin > > r ; 

int r = ( r < < r ) ; 

for ( int r = r ; r < r ; r + + ) 
{ cin > > r [ r ] ; 
r [ r ] = r ; } 
for ( int r = r ; r < r ; r + + ) { int r ; 
cin > > r ; 
if ( r ) 
r [ r ] = true ; 
else 
r [ r ] = false ; 
} 

r ( r , r - r ) ; 
for ( int r = r ; r < r ; r + + ) 
cout < < r [ r ] < < r r ; 
cout < < r ; 
for ( int r = r ; r < r ; r + + ) 
{ r [ r ] = r ; 
r [ r ] = r ; } 

r ( r , r , r , r ) ; 
r ( r , r , r ) ; 
for ( int r = r ; r < r ; r + + ) { 
if ( r [ r ] ) 
cout < < r [ r ] < < r r ; 
else 
cout < < r [ r ] < < r r ; 
} 

cout < < r ; 


return r ; 
} 


